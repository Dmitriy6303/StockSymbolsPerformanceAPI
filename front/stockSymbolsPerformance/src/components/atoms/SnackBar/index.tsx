import React from 'react';

import './styles.css';

export default class SnackBar extends React.PureComponent {
  render() {
    return (
      <div id="snackbar">You need to select one of the tabs</div>
    );
  }
};

